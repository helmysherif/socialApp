<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
        integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.css" integrity="sha512-3pIirOrwegjM6erE5gPSwkUzO+3cTjpnV9lexlNZqvupR64iZBnOOTiiLPb9M36zpMScbmUNIcHUqKD47M719g==" crossorigin="anonymous" />
    <link rel="stylesheet" href="/css/toastr.css">
    <title>Registration</title>
</head>

<body id="login">

    <div class="overlay">

        <!-- ======================Registration form================================= -->
        <div class="container pt-5">
            <div class="row">
                <div class="col-md-7 m-auto p-5">
                    <div class="form p-5 ">
                        <h2 class="text-center text-white">Registration</h2>
                        <% if (exists.length != 0) { %>
                            <div class = "alert alert-danger my-3 text-center">
                                <h6 class = "m-0 p-0">email already exists</h6>
                                <%= req.toastr.render() %>
                            </div>
                        <% } %>
                        <form action = "/register" method = "POST">
                            <div class="row mt-3">
                                <div class="col">
                                    <input value = "<%= oldInput.length == 0 ? '' : oldInput[0].fname %>" type="text" class="form-control inp pt-3" placeholder="First Name" name = "fname">
                                    <% if (errors.find(e => e.param == 'fname')) { %>
                                     <div class = "alert alert-danger my-3 text-center">
                                        <h6 class = "m-0 p-0">Field Is Required</h6>
                                     </div>
                                    <% } %>
                                </div>
                                <div class="col">
                                    <input value = "<%= oldInput.length == 0 ? '' : oldInput[0].lname %>" type="text" class="form-control inp pt-3" placeholder="Last Name" name = "lname">
                                    <% if (errors.find(e => e.param == 'lname')) { %>
                                        <div class = "alert alert-danger my-3 text-center">
                                           <h6 class = "m-0 p-0">Field Is Required</h6>
                                        </div>
                                    <% } %>
                                </div>
                            </div>
                            <div class="form-group">
                                <input value = "<%= oldInput.length == 0 ? '' : oldInput[0].username %>" type="text" class="form-control inp pt-3" placeholder="Username" name = "username">
                                <% if (errors.find(e => e.param == 'username')) { %>
                                    <div class = "alert alert-danger my-3 text-center">
                                       <h6 class = "m-0 p-0">Field Is Required</h6>
                                    </div>
                                   <% } %>
                            </div>
                            <div class="form-group">
                                <input value = "<%= oldInput.length == 0 ? '' : oldInput[0].email %>" type="email" class="form-control inp pt-3" placeholder="Email" name = "email">
                                <% if (errors.find(e => e.param == 'email')) { %>
                                    <div class = "alert alert-danger my-3 text-center">
                                       <h6 class = "m-0 p-0">Field Is Required</h6>
                                    </div>
                                   <% } %>
                            </div>
                            <div class="form-group">
                                <input value = "<%= oldInput.length == 0 ? '' : oldInput[0].password %>" type="password" class="form-control inp pt-3 mb-3" placeholder="Password" name = "password">
                                <% if (errors.find(e => e.param == 'password')) { %>
                                    <div class = "alert alert-danger my-3 text-center">
                                       <h6 class = "m-0 p-0">Field Is Required</h6>
                                    </div>
                                   <% } %>
                            </div>
                            <button type="submit " class="btn btn-outline-info text-white w-100">Sign Up</button>
                            <p class="text-center pb-3"> <a href="/login" class="text-white mt-3 d-block">Sign In Here</a>
                            </p>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>














    <%- include("partials/footer.ejs")  %> 
